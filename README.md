# Cash file uploader

Upload file hashes to the BCH testnet blockchain

## Requirements

- python3.8

## Instructions:

- Run Electron-cash:

```
docker-compose build
docker-compose up
```

- Run the API:

```
pip install -r requirements
./run.sh
```
